name: Deploy to server
on:
  push:
    branches:
      - main

jobs:
  first_jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World step
        run: echo "Hello, World!"

  secound_jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World step
        run: echo "Hello, World!"